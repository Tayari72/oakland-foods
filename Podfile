# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

source 'https://github.com/CocoaPods/Specs.git'
target 'oaklandfoods' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for oaklandfoods

  target 'oaklandfoodsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'oaklandfoodsUITests' do
    inherit! :search_paths
    # Pods for testing
  end
pod 'Firebase/Core'
pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'Firebase/Auth'

end
